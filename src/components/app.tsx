import { FunctionalComponent, h } from "preact";
import Header from "./header";

const App: FunctionalComponent = () => {
  return (
    <div id="preact_root">
      <Header />
    </div>
  );
};

export default App;
